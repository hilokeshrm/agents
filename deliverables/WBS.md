# OppTrack Work Breakdown Tracker

Living mirror of [`../docs/02-architecture/OppTrack_Work_Breakdown.html`](../docs/02-architecture/OppTrack_Work_Breakdown.html) (15 packages, 108 tasks). That HTML stays the canonical spec for full task descriptions, Done-when criteria and dependency rationale -- this file exists to be a fast, greppable checklist that gets updated in place as work lands in `deliverables/`.

**How to keep this current:** when a task is completed here, flip its Status to `Done` (or `Partial` with a one-line note) in the table row below, and add a dated line under **Recent changes**. Do not renumber or reorder tasks -- IDs must keep matching the HTML.

Status values: `Done` · `Partial` · `To do` · `Blocked`

## Recent changes

- 2026-09-17 (evening) -- **Sign-in, and what testing by hand turned up.** Lokesh asked for sign-up, login, a landing page and Gmail OTP. The decision register says roles come only from an admin-provisioned row and there is no self-registration, so it is built as *self-service credentials, admin-assigned roles*: `AUTH_MODE=local` adds `auth_account` / `auth_session` (migration 0010), `app/security/local_auth.py` (PBKDF2 passwords, hashed six-digit codes with a 10-minute life, five attempts and a 45-second resend gap, opaque revocable sessions, reset by code), `/api/v1/auth/*`, `app/services/mail.py` (console or SMTP -- Gmail with an App Password), and the screens: landing, sign-up, verify, sign-in, reset, "waiting for a role", sign-out in the profile card. A verified account with no provisioned row can sign in and is told to ask an admin; the dev headers are ignored in local mode. `scripts/dev_up.ps1` now runs local mode by default (`-Mode headers` for the per-role servers); `scripts.seed_demo_accounts` gives the five demo people a password. Found while writing the manual test plan (`docs/manual-test-plan.md`): intake accepted a Lost row with weighted revenue and no loss reason (now refused: reason required, confidence 0.00, reason on the seed event and as V25 provenance); a Korea-scoped manager could create a Europe row (now 403); the sidebar showed a fixture identity (now the resolved one, with the headers it used). The UI test gained a sign-in stage: 73 steps. 372 tests.
- 2026-09-17 (later) -- **End-to-end UI test, and what it found.** `python -m scripts.ui_test` builds the walkthrough database, starts the backend and one Vite server per role, and drives installed Chrome through Playwright as director, owner, manager, finance and admin: 56 checks over intake, the review queue (reject with a reason, approve), roll-ups, audit, import, the findings report, analyses, notices, the assistant (answer with citation, refused write), the embedded route, the rubric, users, connectors dry run, access and run comparison, with screenshots under `ui-report/` and a `report.json`. It fails on any console error or 5xx/404 API response. It found three real defects that the API tests could not: the frontend dropped `reason_code` unless the action was an override, so every rejection (and every Design Lost approval) was refused with 400; the run comparison view queried `/runs//compare/` before the run list had loaded; and the console's "need review" pill did not drop after a decision because the shell kept its own copy of the queue. All fixed, plus a stale "6-factor" label (13 rules), the boot log's "0 projects" line, a missing favicon, and the workbook report now cached by file mtime. Separately, a flaky ordering in the assistant history (two conversations logged in one Windows clock tick) is fixed by a strictly increasing stamp and an id tiebreak. 357 tests, 56/56 UI steps, 0 console errors.
- 2026-09-17 -- **The walkthrough, CI, and one UI fix.** `python -m scripts.walkthrough` is the section 09 deliverable ("walkthrough for David and Gene"): from the real workbook into a fresh, discarded database it writes `docs/walkthrough.md` -- the findings, the nine rows before, the ten-step run, every proposal with its rules and flags, and Hermes 0.30 -> 0.20 ($12,000K -> $8,000K) approved by a director with the append-only stream behind it. That is the build specification's own worked example (section 06) reproduced end to end; a test pins it. One liberty, stated in the document: the workbook has no owner column, and without one J-12 haircuts every row by 10pp, so the walkthrough assigns a nominal owner per region -- which is the case for the owner field. `.github/workflows/ci.yml` runs the suite, the findings report, the walkthrough, a generated-docs freshness check, the migration up/down/up, the frontend typecheck and build, and the container builds. Fixed a stray bodiless `fetch` in the Analyses target form. Registered `market_programme` on `Base` explicitly (it had only been reachable through the connectors import). 357 tests.
- 2026-09-15 (later) -- **The remaining 67 tasks.** Built in eight batches, 356 tests (from 292), the live dev DB migrated to 0009 and its nine rows renumbered OPP-000001..9. Every task is now Done; six of them are complete *mechanisms whose data is an input nobody on the team can produce*, and the WBS row says so rather than pretending otherwise: coverage (Finance's targets), forecast accuracy and owner calibration (ERP/POS actuals and two quarters of time), the EAU cross-check and white space (the vehicle-data licence), hosted-model use (the security sign-off -- `JUDGMENT_MODE=mock` until it exists), and the ROI feed (the ROI Agent side adopts the contract). Each lights up on its own the day the input lands, because runnable() names the parameter it waits on.
  - **A** registry, intake, calc (1.5, 2.1, 3.7, 4.4, 4.6, 5.4-5.6): 61 ParamSpecs; the intake schema as code with decision #62's tiers; one intake service under both paths; the SoP-date cross-tab check; rules wired into the import dry run; C10 by quarter proven against FCST_Revenue; the three-tier phasing rule with per-opportunity profiles; C3-C12/T2/C10 regression-tested against the specification's worked examples. Found and fixed a units bug on the way: `channel_margin_pct` is a fraction, and J-13's thresholds had been comparing it against 15 and 2.5.
  - **B** analyses and alerts (6.1-6.6, 8.5, 11.7): a discovering registry with a per-run availability set; nine analyses (six live on today's rows, three dark by name); Finance's `target` table; the `actual` table; the quarterly calibration rebuild with sd, sample size and the two-quarter reliability rule; run-to-run comparison with attribution; the nightly stall/milestone sweep writing findings and notices, de-duplicated per row per day; notifications with delivery state over log/SMTP/webhook.
  - **C** identity, retention, memory (9.3-9.5, 9.7, 10.1, 7.5): OIDC with role and scope from the provisioned row, service accounts by hashed key, anonymise-on-delete, request ids, and L3 memory that scrubs numbers and pricing before storing and filters by region/product line/part/recency before it ranks.
  - **D** connectors (11.1-11.6): the framework, decision #63's ladder (CRM and ERP were the wrong way round in the old trust table -- corrected), reconciliation items in the review queue, and four file-shaped connectors. A blank field is filled by any connector; a human edit is never overwritten by one.
  - **E** interfaces (10.9-10.13, 1.6, 14.6): the MCP server over the same nine tools, signed webhooks with retry, CSV exports, the ROI feed and cross-check, and the SPA screens -- role landings, findings report, analyses with target entry, connectors and the reconciliation queue, notices, users, run comparison, and Ask OppTrack in the app and at `/embed/assistant`.
  - **F** operations (13.1-13.6): Dockerfiles and compose (written, not run here), eight scheduled jobs with a crontab, the vLLM fallback with the Western-weights-only check, month counters and `/metrics`, backup/verify/restore with a tamper check, and a monthly budget that refuses live calls loudly.
  - **G** assistant (14.1-14.5, 12.7): grounding (a figure no tool returned flags the answer and is named), refusal with a pointer for writes and out-of-scope questions, what-if never refused, every conversation logged.
  - **H** proof and docs (12.4, 12.5, 15.1-15.7): the reproducibility and authorisation suites; the reviewer handbook, the sales-owner guide, the connector guide, the architecture and audit pack, the rollout plan; and `python -m scripts.generate_docs` for the pages that must not drift from the code.
- 2026-09-15 -- **David Nam did not answer; the decision register adopted, and the chain behind it built.** 1.1 sat Blocked since August with five tasks behind it. The register (`docs/OT_Decision_Register_and_Claude_API_Purpose.md`, 2026-09-09) already held provisional answers to every question; this pass adopts them as policy (its new section 7 records the adoption and the four things it had to add), marks everything built against them provisional, and closes the chain. 292 tests, up from 218; the live dev DB migrated to 0008, v1 published as `2026.1-r2` (the label `2026.1` was taken by the 10 Sep six-factor version), one run executed. What landed, in build order:
  - **Region and reason codes** (1.1, 2.6): `North America` -> `US` (provisional); loss reasons aligned to #43, a `rejection_reason` vocabulary added for 8.7.
  - **Rubric v1 as one published version** (2.3): `publish_v1()` / `POST /rubric/publish-v1` -- Matrix A baselines, Matrix B with per-rule caps, 0.05-0.95, a 20pp run cap (#19), lifecycle bands derived from the baselines (early < 0.65 <= design_in < 0.90 <= design_win < 0.95 <= mass_production, so Apollo 0.50 -> 0.66 crosses, the spec's example), and `review_policy: gate_all` (#21). The policy is a published setting: `gate_band_crossing` is Plate 2's step 9 and needs no code change.
  - **Judgment layer** (7.1-7.4, 7.7, 7.9, 12.2, 12.3): `app/judgment/matrix_b.py` is the thirteen-rule table; `contract.py` is the typed proposal that cannot exist out of bounds; `rubric.py` reads Matrix A as a flag on the bundle and fixes the live reply shape with a JSON schema on the request (`output_config.format`), checks `stop_reason`, picks the text block by type, and raises on every failure -- a MOCK fallback is recorded on the run (`live_fallbacks`) and the run marked mixed. Two deterministic guards (`matrix_a_contradiction`, `not_early`) make the Aphrodite/Montana misfires structurally impossible; a quote must be a verbatim substring of the bundle text the model was shown. Portfolio context (`app/services/portfolio.py`, also C12) lets J-05 fire: over the nine spec rows it fires on six, because Ford is 60% of the weighted pipeline and Korea 81% -- the honest reading of that portfolio. A forbidden pairing is blocking at step 2 (#10), not a haircut.
  - **Findings engine** (3.1, 4.1, 4.3, 4.4, 4.5, 12.6): `app/intake/xlsx_reader.py` reads the real workbook including the two-row header, the forward-filled FCST quarter groups and the roll-up footers with their formulas; `app/guards/rules.py` is V-a..V-i; `python -m scripts.findings_report` reproduces all twelve documented findings from `TrackF (1).xlsx` in one command -- the Phase 0 demo every architecture doc named -- and finds two the spec missed (ID802 at 36.8M and ID803 at 35.0M vehicle sets a year, the same defect as IP901). Both intake paths refuse a forbidden pairing identically, falling back to the template Matrix A when nothing is published.
  - **Review loop** (8.1, 8.2, 8.4, 8.6, 8.7): `app/db/immutable.py` refuses UPDATE/DELETE on the four audit tables at the ORM for every role (a test that backdated a row by mutating it now goes through a Core statement, the documented gap); a rejection needs a reason code and can name the factors it disagrees with, aggregated per rule at `GET /rubric/feedback` and shown on the rubric screen; recompute-on-approval asserted identical to the engine; Lost needs a reason code from the register's list.
  - **Lifecycle** (8.3): `app/services/transitions.py` -- the agent proposes Mass Production for a Design Win at PVT past its M/P date, or Lost when the row's evidence says so, into the same queue; approval needs the close grant and goes through `record_transition`; a Lost approval needs the loss reason. On the live DB the first run proposed Hercules -> Mass Production (M/P Sep'26) and blocked Montana, whose row had been left at Evaluation/DVT by earlier UI testing -- a forbidden cell, correctly refused.
  - **Opportunity ID** (2.4): `OPP-000001` from `opportunity_sequence` at both intake paths; a supplied CRM/workbook id is preserved. Existing rows keep their ids until 9.8's crosswalk.
  - **UI**: review console handles transition proposals, rejection reasons, bounds flags, rule ids and quotes; rubric screen shows per-rule caps, run cap, review policy, a publish-v1 button and 8.7 feedback per factor. Typechecks and builds.
  - **Still open from this pass**: 4.4's SoP-date disagreement check; 12.4 reproducibility; 6.1/6.2 analyses; 9.4 SSO; the security sign-off for hosted-model use (#76) -- `judgment_mode` stays `mock` until it exists.
- 2026-09-10 -- **Rubric, Import and Access screens** (WBS 10.7, 10.11, 10.8). Three endpoint families the backend had served for weeks with nothing in the UI able to reach them.
  - **Rubric & Matrix A** (`RubricView.tsx`): the grid as an editable table -- a baseline per (design status, stage) pairing, a toggle that marks a pairing forbidden, and the distinction the whole table turns on kept visible: a dash is a pairing the rules forbid, an empty cell is undecided, and the two are not the same. Factors and caps are editable, a cap left unset clips nothing, and publishing stamps a version every later proposal cites. Verified live: published 2026.2 with two cells set while 2026.1 kept its nine proposals -- publishing re-runs nothing, which is the property the table exists for. That verification version was deleted afterwards.
  - **Import wizard** (`ImportView.tsx`): choose a file, dry run, read the findings, commit. The commit sends the snapshot id rather than the file again, so what is written is what was approved. Verified with a three-row CSV: 1 would create, 2 blocked, with the real findings behind them -- `CANON` refusing to invent a mapping for "North America", `COERCE` turning TBD into null rather than zero, and `REQUIRED` on a skeleton row. The verification snapshot was deleted afterwards.
  - **Users & access** (`AccessView.tsx`): the permission matrix, the six enforcement points with what is actually built at each, and the identities observed in the data. It states plainly that it is not a provisioned user list and why there is no Provision user button.
  - **The sidebar is now role-aware.** Each entry names the permission the API checks, and a screen a role cannot use is removed rather than disabled. Verified by switching identity: an admin sees Dashboard, Pipeline, Roll-ups, Runs & Audit, Rubric and Access -- no Intake, no Review Queue, no Import; an owner sees Intake and Import but no Review Queue, Rubric or Access. That is the matrix, rendered.
  - **A permission boundary the screens made visible rather than papered over:** the admin role has no view grant on opportunities, so the rubric impact preview -- a pipeline read -- legitimately returns nothing for the one role allowed to publish. It first rendered as "$0K, nothing changes", which reads as a finding rather than a boundary. The pane now says which grant is missing and which roles can run it. **This is a product decision waiting to be made, not a defect:** either an admin drafting policy should be able to measure it, or impact review belongs with a director. Nothing was widened here to make the screen look better.
  - `tsc --noEmit` clean; backend unchanged at 218 tests. The `workflow-*`, matrix, import and access styles were added to `styles.css`.
- 2026-09-10 -- **NRE separated from silicon revenue in the forecast pipeline** (WBS 3.5, 5.4). `app/calc/nre.py` had been splitting the workbook's own NRE rows since August, but an opportunity had nowhere to record a charge of its own, so any NRE entered against one would have had to hide inside EAU x ASP and phase like silicon in every downstream quarter.
  - **`opportunity.nre_charge_k`** (migration 0007, ParamSpec **V29**): nullable, because most rows carry none and a null means "no NRE here" rather than a defaulted zero. Registered as *proposed* rather than present -- the workbook carries NRE only as whole FCST_Revenue rows, never as a per-opportunity field -- and wired through canonicalisation and the provenance wrapper, so the charge reaches the calc engine with a source attached like every other value.
  - **The calc engine** carries `nre_revenue_k` and `nre_weighted_k` beside `sales_revenue_k`/`adjusted_revenue_k` and never inside them; `portfolio_rollup` totals them separately and adds an `nre_revenue_k` column to every group, so a regional total cannot quietly omit it. Both the entered charge and a confidence-weighted view are returned, because whether an unwon design's NRE should be weighted is a policy question this code does not answer.
  - **The forecast feed** recognises NRE whole in the M/P quarter instead of spreading it over the 10/20/30/40 ramp: the ramp models silicon shipping into a programme, and a one-off engineering charge does not ramp. It is reported in its own quarterly column with `nre_rule: recognised_whole_at_mp_quarter`.
  - **UI**: an NRE field at intake, the charge on the opportunity detail as a separate formula line with the reason it is separate, and an NRE row in the roll-ups summary.
  - 8 new tests (218 total). The one that matters is `test_adding_nre_moves_no_volume_figure`: NRE must be additive information, not a change to any number that existed before it. Verified on the running app by entering a $126K charge through the intake form -- stored with V29 provenance, reported at $126K beside an unchanged $1,500K of volume revenue, and absent from the weighted total. The verification row was deleted afterwards.

  **Two defects found and fixed on the way:**
  - **`weighted_forecast_k` was multiplying by confidence twice.** `app/api/v1/dashboard.py` computed `adjusted_revenue_k * confidence`, and `adjusted_revenue_k` is already amount x confidence -- so the headline "Weighted forecast" tile reported sales x confidence squared. On the real nine rows that was $16,584.5K against a true $30,915K. Fixed, and the roll-up test now asserts the figure against the sum of the rows rather than trusting the label.
  - **Seven call sites built the calc engine's input row by hand**, and had drifted: none passed `end_customer` or `product_line`, so every roll-up grouped by those dimensions reported "Unspecified" for the entire portfolio. Consolidated into `app/services/financials.py` (`engine_row` / `financials_for` / `adjusted_revenue_k`), which is also why NRE reached all seven at once instead of being added seven times.
- 2026-09-10 -- **Loss reason and override, reachable from the UI.** The lifecycle gate and the override vocabulary were enforced at the API with no screen able to satisfy them: the SPA posted `{action}` only, so an override could not carry a value or a reason code, and nothing called `POST /opportunities/{id}/transition` at all, so a status could not be moved and a loss reason could not be supplied.
  - **`GET /registry`** (new, `app/api/v1/registry.py`): the canonical design statuses, stages, approval-required statuses and the three reason-code vocabularies, each with its `ratified` flag. It exists to remove a class of bug rather than to add a feature -- a screen that hardcodes these lists is one backend edit away from offering a value the server rejects. The lifecycle gate's status set moved out of an inline literal in the route into `APPROVAL_REQUIRED_STATUSES` in `app/services/state_transitions.py`, so the route and the endpoint read one constant.
  - **Review queue** (`WorkflowViews.tsx`): a decision panel per proposal -- the factors that fired with their guards, the band note, an optional note written to the audit event, and Approve / Override / Reject. An override carries the reviewer's value and a code from the served vocabulary, and the panel says the vocabulary is draft while the API enforces it.
  - **Lifecycle controls** (`LifecycleControls.tsx`, new): design status and stage moves through the sanctioned write path. Terminal statuses are labelled "(needs approval)" from the served list, disabled with the reason when the role lacks the grant; `Lost` requires a loss reason before the request is made. `actingIdentity` is exported from `api.ts` so a write records the identity the request was authorised under rather than a blank actor.
  - The `workflow-*` classes the review and audit views were already using had **no CSS at all** -- those rows had been rendering as bare divs. Added, plus the decision panel and lifecycle styles.
  - 5 new API tests (210 total, all passing), `tsc --noEmit` clean. Verified on the running app against the nine backfilled rows: overrode Dresden 0.50 -> 0.40 with `new_evidence_since_proposal` and a note (audit event recorded actor `marc.v`, role `director`), and moved Poseidon to Lost with `program_cancelled`, which wrote the state_history event and moved win rate to 2/3. Both writes were rolled back afterwards -- they were verification, not decisions.
  - **Found while verifying: migration 0006 had never been applied to `opptrack.db`.** Every opportunity read was returning a 500 (`no such column: opportunity.external_id`) and the dashboard rendered $0K. The test suite could not catch it because tests build the schema from metadata rather than through Alembic. Fixed by running `alembic upgrade head`.
- 2026-09-07 -- **The rest of the UI**: the six screens package 10.0 named and nothing had built yet -- review console (10.5), rubric and Matrix A admin (10.7), users and access (10.8), runs and audit viewer (10.9), per-role landing views (10.10), import wizard and findings (10.11) -- plus the assistant, the roll-ups screen, and the mechanisms each one needed to be a reading of stored facts rather than a mock. Logged in full here rather than split thin across rows, the same way the 2026-08-31 UI port was.

  **Backend, because a screen with nothing behind it is a mock:**
  - **`app/security/roles.py`** (WBS 9.5, partial): the product document's permission matrix transcribed cell for cell, and enforced -- `require()` asserts the role before the handler runs, `scope_opportunities()` wraps the SELECT so an out-of-scope row is a 404 rather than a filtered response, and `app/services/proposals.py` refuses a reviewer resolving a row they own. What is *not* real is the identity source: role and region scope come from request headers until SSO lands (9.4), which is stated in the module docstring, on the Users & access screen, and in the identity switcher itself rather than dressed up as a login. Defaults match the previous unauthenticated behaviour, so nothing built before this changed.
  - **`app/services/rubric_versions.py`** + **`app/api/v1/rubric.py`** (2.3/7.1 mechanism, behind 10.7): draft, publish, version. Matrix A ships as an *empty* grid -- every canonical (design status, stage) pairing with its baseline unset -- because baseline confidence is 1.1 Q1 and unratified; a pre-filled grid would read as agreed the moment it rendered. Caps likewise start unset, so nothing is clipped until an admin publishes a version that says to.
  - **`app/judgment/reply_guards.py`** (step 6 of Plate 2): the eight guards, with `FACTOR_REQUIRES` and `FACTOR_DIRECTION` derived from each factor's own guidance text in `rubric.py`, not invented. A factor whose inputs are absent reports *not assessable* -- the same discipline `runnable()` applies to an analysis -- and a rejected factor is kept on the proposal and shown, because a reviewer who only sees what the model got right cannot calibrate the next one.
  - **`app/services/judgment_runs.py`** + the twelfth table, **`run`** (10.9): the ten-step trace executed and stored. Steps 4 (Precedent) and 8 (Lifecycle) report `skipped` with the reason -- `app/memory/` is empty, and no lifecycle bands are ratified -- exactly the two dashed steps in Plate 2. A run over live rows records `snapshot_id = null` and says so at step 1 rather than fabricating a snapshot. A failed run is written and kept.
  - **`app/services/proposals.py`** + **`app/api/v1/review.py`** (7.0/8.1/10.5): raise, queue, and resolve. The evidence bundle is a whitelist that excludes C1/C2 -- the model receives no computed revenue. An override must cite a code from the `override_reason` vocabulary (2.6), enforced at the API, which is the first thing to consume that vocabulary for real. Every resolution writes a `confidence_event` with actor and role before it returns, and a proposal with `band_crossing = null` queues rather than auto-applying.
  - **`app/services/imports.py`** + **`app/api/v1/imports.py`** (3.1/4.5/4.6, behind 10.11): dry run seals the file into a snapshot and persists its findings; commit re-reads those bytes from object storage, re-checks the sha256, and creates only the rows that passed -- so what is written is what a person approved, not a second upload. Imported values carry `trust = 0`, below every connector.
  - **`app/api/v1/scenarios.py`** (5.7): scenario recompute (pure, nothing persisted) and the forecast feed. The feed states its own assumption -- the quarterly phasing rule is 5.5 and undecided, so nothing is spread across quarters and a row with no M/P date is reported unphased rather than assigned to one.
  - **`app/services/assistant.py`** + **`app/api/v1/assistant.py`**: nine read-only tools over the same tables, no write tool of any kind, region-scoped like every other reader. Live mode is a tool-use loop on `claude-opus-5` with adaptive thinking; the default is a deterministic reader that answers what it recognises and says plainly when it does not, rather than guessing. `app/judgment/rubric.py`'s live scorer moved to the same model (it was pinned to `claude-sonnet-4-5`).
  - **`GET /opportunities/{id}/provenance`** (3.6): the read that makes the stored provenance usable -- highest-trust value per ParamSpec id, with the lower-trust ones it held back.
  - Migration **0005**: `run`, `proposal.run_id` / `band_crossing`, `confidence_event.actor_role` / `reason_code` / `note`.

  **Frontend** -- 9 new screens plus 3 reworked, all against the live API, no mock data: `/review`, `/runs`, `/runs/[id]`, `/import`, `/assistant`, `/admin/rubric`, `/admin/access`, `/roll-ups`, a per-role `/` landing, and Judgment + Provenance tabs on the opportunity page. `AppNav` is role-aware and *removes* what a role cannot use rather than disabling it -- a disabled approve button teaches an owner that approving is part of their job and someone is withholding it. The Opportunities list gained a Matrix A baseline column, which turns it into a deviation report once a grid is published.

  **Verified against the real nine backfilled workbook rows, on the running app, not only in tests:** published a rubric version, triggered a run (9 rows, 4 factors fired, 18 not assessable, 0 rejected, `-$1,950K` if all applied), and drove the full review loop from the browser as a Europe-scoped manager -- approving Dresden moved it 0.50 -> 0.35, its weighted revenue $1,500K -> $1,050K, and wrote an `approval` event stamped `priya.n` / `manager`; that verification was then rolled back so the row sits where the backfill left it. Region scope proven by observation rather than assertion: the same assistant question answers `$30,915K` across three regions as a director and `$2,175K` across two rows as the Europe manager, because the rows are never fetched. 68 new backend tests (195 total, all passing), `npx tsc --noEmit` clean, all 17 routes checked live.

  **Two real bugs found and fixed during that pass**, both invisible to the test suite: the run's step-6 line counted not-assessable factors as rejected ones (they are now disjoint properties, with a test that says why -- reporting them together tells a reviewer the model was wrong 18 times when it was silent 18 times); and the identity switcher read `localStorage` during the initial render, which hydrated against server HTML that could not see it.

  **Still not built, and unchanged by this pass:** 10.12 MCP server, 10.13 webhooks and exports, the connectors (11.0), auth itself (9.4/9.5), the V-a..V-i rule set (4.1), the six live analyses (6.2), and L3/L5 memory. The screens that depend on those say so on their face rather than filling the space.
- 2026-08-31 -- Dev Tools (`/dev`, not a WBS task -- local debugging aid on request). `app/api/v1/dev.py`: `GET /dev/tables` and `/dev/tables/{name}` browse every real DB table via SQLAlchemy's inspector (no hardcoded table list, so a new model shows up automatically); `GET /dev/logs/{backend|frontend}` tails the actual redirected process output from `deliverables/logs/`, not a synthetic feed. Caught and fixed a real bug before it shipped: the first draft queried a raw global `engine` import instead of going through `Depends(get_db)` like every other router, which silently bypassed the test suite's DB override -- `test_get_table_returns_columns_and_rows` would have been asserting against an empty, unrelated database. Fixed to use the same dependency-injected session pattern as the rest of the app; 5 new tests, all passing. Unauthenticated by design (WBS 9.4 SSO doesn't exist yet) -- explicitly not for anything past a local machine.
- 2026-08-31 -- Full CRM-style UI port from `prototypes/opportunity_tracker.html` (user's explicit choice among three offered scope options). This is a large, cross-cutting pass -- not one WBS task -- so it's logged here in full rather than split thin across many one-line entries.
  - **Schema, closed properly, not worked around:** `application`, `product_line`, `mp_date` added to `Opportunity` (migrations `0003`, `0004`) -- all three were already registered in `app/registry/parameters.py` (V5, V6, V10) but never reached the DB, same gap pattern as `unit_set` earlier. Full canonicalize/provenance wiring extended to match. A new `contact` table (11th table; linked to opportunities by account *name*, not FK -- there is no `account` table, since accounts are a computed rollup, not a stored entity). All three new migrations tested up/down individually and as a full chain.
  - **New backend surface, all real rollups over live rows, nothing fabricated:** `app/api/v1/accounts.py`, `products.py`, `reports.py`, `dashboard.py` (computed, no new storage beyond `contact`), `contacts.py` (real CRUD, starts empty -- the workbook has no person-level fields, matching the prototype's own "nothing invented" convention). 15 new tests (`test_api_contacts.py`, `test_api_rollups.py`), all passing.
  - **9.8 One-time backfill, now Partial** (was To do): `scripts/backfill_real_data.py` reads ProjectTrack's nine complete rows directly from the real workbook through the live `canonicalize -> provenance` pipeline (`source=import`, lowest trust tier). Deliberately does **not** backfill Funnel's eight rows -- the source document is explicit that Funnel has no Stage and no Confidence at all, both NOT NULL columns, and inventing either would break the exact discipline every other module in this codebase holds. `owner="Unassigned"` and a confidence-rationale placeholder both state their own absence rather than inventing plausible content, matching the prototype's own convention for the same gap.
  - **Design system**, ported faithfully from the prototype (`frontend/app/globals.css`): same color tokens, same component vocabulary (card/tile/table/pill/kanban/path/tabs/feed/field-grid). The prototype's decorative Salesforce chrome (waffle menu, launcher, notification popovers, modal/toast machinery) was deliberately left out -- this is a real, backend-wired app, not a demo shell, and that chrome carried no real content.
  - **Pages**: Home, Opportunities (restyled), Stage Board (restyled, still hardware Stage -- Concept/EVT/DVT/PVT -- kept deliberately separate from Design Status, which the prototype's "stage" pipeline concept actually maps to), Opportunity detail (rebuilt with a clickable path driving real `design_status` transitions through the same WBS 9.2 endpoint the stage board uses, plus Details/Activity/Related tabs), Accounts (list + detail), Contacts (list/create/delete), Products (list + detail), Reports (list + run, 6 real groupings), Dashboards (4 charts, all from `/dashboard`). Intake form gained the three new fields. `roll-ups` and `assistant` (the original WBS-planned screens, not part of the prototype) are left as unlinked stubs, not deleted -- they're separately real future work per `docs/02-architecture/OppTrack_Product_Surface.html`, not part of this scope.
  - **Verified, not assumed**: full backend suite green (120/120) after every schema/endpoint change; `npx tsc --noEmit` clean across the whole frontend; every one of the 12 routes checked live (200, clean compile, no console errors in the dev log); the real functional loop proven end to end against the live servers -- moved Montana from Design In to Design Win through the exact API call the new path component makes, confirmed Dashboards' `closed_won_k` moved by exactly Montana's adjusted revenue ($600K), then reverted it.
- 2026-08-31 -- 6.3 Milestone extractor: **Done**. `app/intake/milestones.py`: `extract_milestones()` pulls PPAP/ES/Design Review/SoP dates out of Funnel's free-text "Next Action To Do" column, reusing `app/intake/coerce.py`'s `coerce_date` for the actual parse rather than duplicating it. A caught bug before it shipped: a naive `"ES" in text` containment check would have flagged every "Design Review" mention as an ES milestone too, since "ES" is a literal substring of "DESIGN" -- fixed with `\b` word-boundary regex, and a dedicated test (`test_design_review_does_not_false_positive_as_es`) guards the fix. `overdue` is computed against an `as_of` date passed in by the caller, never read from the clock, so extraction stays pure. Proven against the real workbook's own cited example (`"NX5 SoP: Sop'26. Design Review: Jul'25, PPAP: Oct'26"`): Design Review and PPAP parse to real dates, SoP does not because `"Sop'26"` is the file's genuine typo for `"Sep'26"` -- reported, not corrected, the same discipline `coerce.py` and `canonicalize.py` already apply. The done-when is asserted directly: every extracted event's `source_substring` is checked to actually appear in the cell text it came from. Not wired anywhere live yet -- its consumer is WBS 7.2 (rubric factor J-10, slip detection), part of the judgment layer, still to do.
- 2026-08-31 -- 4.2 Blocking vs advisory severity: **Done**. `app/calc/severity.py`: `rollup_with_severity()` proves the literal done-when -- an included total, an excluded count, never blended. `classify_severity()` is a minimal real example rule (missing Customer or Disty ASP is blocking, since `compute_project_financials` cannot even run without them), not the WBS 4.1 rule set, which is still to do. Tested against the real workbook's own cited example: ProjectTrack's five skeleton rows (14-18, Design Win/Promotion entries with a Region and an EAU but no Customer, Project, Disty ASP or Confidence -- "design wins worth zero" per the field map doc) are correctly excluded, and the included total is exactly the nine complete rows' sum (`$82,350K` sales / `$30,915K` adjusted, hand-verified against the same O/R columns `test_calc_engine.py` already validates row by row) -- not diluted by the zeros. Also proves *why* exclusion has to happen before computing, not after: calling `compute_project_financials` directly on a skeleton row raises `TypeError` (no Disty ASP to multiply by). Not wired into a live endpoint: the current API already rejects anything skeleton-shaped at the Pydantic/canonicalisation layer, so no persisted opportunity could exercise this rollup path today; its real consumer is a portfolio-wide rollup endpoint, not yet built.
- 2026-08-31 -- 3.5 NRE line splitter: **Done**. `app/calc/nre.py`: `split_nre_from_volume()` separates FCST_Revenue rows by Design Status into `VolumeRevenueRow`/`NRECharge` (NRE gets its own record type rather than hiding inside ordinary silicon revenue, since its ASP-forced-to-1 shape would otherwise phase wrong once WBS 5.5 exists); `reconciles_to_original()` proves the literal done-when. Tested against the real workbook, not transcribed values -- the test reads FCST_Revenue's ten rows directly via openpyxl (same approach as the 3.4/3.3 real-file tests) and confirms the exact 7 NRE / 3 volume split WBS 3.5's own description states, full reconciliation, and the two figures the field map doc cites by name ($126K, $210K, both matching the CY25 total column exactly). Not wired into a live endpoint -- FCST_Revenue isn't part of the direct-entry schema; this module's consumers are WBS 3.1 (Workbook reader) and 5.4 (NRE recognition by quarter), both still to do.
- 2026-08-31 -- 3.3 Coercion: **Done**. `app/intake/coerce.py`: `coerce_date`/`coerce_numeric`/`coerce_percentage`, each returning a `CoercedValue(raw, value, note)` so the source string always survives, even when unparseable -- the literal done-when ("every coercion is reversible to the source string, which is what the milestone factor must cite"). `TBD`/`N/A` become `None`, never `0` or a guessed date. Proven against the real workbook (`test_real_mp_dates_from_the_workbook_coerce`, reading Funnel's M/P column directly): every real date value coerces, `TBD` stays null. Also proves the file's own typo -- `"Sop'26"` for `"Sep'26"`, in a milestone note -- comes back unparseable rather than silently "corrected," the same discipline `canonicalize.py` applies to an unmapped region. Not wired into the live intake endpoint: `POST /opportunities` receives typed JSON already, not raw workbook strings, so this module's primary consumer is WBS 3.1 (Workbook reader), still to do.
- 2026-08-31 -- 5.2 C3 to C7: **Done**, and wired end to end, not just in `app/calc/engine.py`. Set volume (C3), attach rate (C4, reconciles with Unit-Set by construction -- `set_volume * attach_rate == eau_kpcs` is asserted directly), channel margin $ and % (C5/C6), and part-level demand (C7, `part_level_demand()`, aggregates EAU across every row sharing a part number). All four of C3-C6 return `None` rather than a guessed figure when their input (Unit-Set or Resale ASP) is absent -- same not-invented discipline as canonicalisation. Exposed a real gap while building this: Unit-Set (needed for C3/C4) was never added to the `Opportunity` schema in 9.1, so this task also added it for real -- `unit_set` column (migration `0002_add_unit_set.py`, applied to the live dev DB with existing rows verified to survive as NULL; full up/down chain tested), `OpportunityCreate`/`OpportunityRead` schema fields, `canonicalize.py` passthrough, `provenance.py`'s param mapping (V12), and the intake form gained a Unit/Set input with the new figures shown on success and on the opportunity detail page. Verified live: created an opportunity with `unit_set=10, disty_asp=3, resale_asp=3.2` and got back `set_volume=110, attach_rate=10, channel_margin_usd≈0.20, channel_margin_pct≈6.25%` -- exactly the hand-computed values.
- 2026-08-31 -- 3.6 Provenance wrapper: **Done**, and wired into the live intake endpoint, not just a standalone tested module. `app/intake/provenance.py`: `wrap_row()` cites the ParamSpec id (`app/registry/parameters.py`) for every field, raising if a field has no registry entry rather than silently skipping it. `POST /opportunities` now canonicalizes first (WBS 3.4), rejects with a clean 400 + structured findings if a required field fails to map (e.g. `region="North America"`, still a genuinely open question -- no request is now stored with a raw or invented category), and persists one `field_value` row per intake field (`source=direct_entry`, `trust=4`) in the same transaction as the opportunity itself. `ParamRegistry` gained a `by_name()` lookup; `canonicalize.py`'s `PASSTHROUGH_FIELDS` extended to cover the three direct-entry-only fields (owner, evidence, confidence_rationale) it was missing. Verified against the live server (restarted to pick up the change, confirmed the existing SQLite data survived): submitted `region="KR", customer="MOBIS", design_status="D-IN"` and got back `Korea`/`Mobis`/`Design In`; submitted `region="North America"` and got a clean 400; read the actual `field_value` rows out of `opptrack.db` directly and confirmed 15 rows, correct param ids, correct values. Only the direct-entry path calls `wrap_row` today -- workbook/CRM/POS connectors (WBS 11.x) will need to call it too once they exist.
- 2026-08-31 -- Opportunity detail page (`frontend/app/opportunities/[id]/page.tsx`, no new WBS id -- reinforces 9.2 and 10.4 rather than opening a new task): surfaces `GET /opportunities/{id}/state-history` and `/time-in-stage`, both done since 9.2 but with nothing in the UI showing them until now. Linked from the dashboard's project column and stage-board cards. Verified against the live servers: fetched the real 4-event history for the test opportunity (seed + a DVT move with a reason code + the move back) and confirmed it matches exactly what the table renders.
- 2026-08-31 -- 10.6 Stage board: **Partial**. `frontend/app/stage-board/page.tsx`: kanban columns by `STAGES` (Concept/EVT/DVT/PVT), each card with a stage select, an optional `STAGE_EXIT_REASON_CODES` select (mirrors `app/registry/reason_codes.py` -- must stay in sync by hand until that's generated), and a Move button calling `POST /opportunities/{id}/transition`, the same sanctioned write path 9.2 built. No drag-and-drop; select+button instead. Verified against the live servers: moved the existing test opportunity PVT -> DVT with a real reason code, confirmed the event and the row's new stage, confirmed an invalid reason code produces the same clean 400 the API test suite already covers, then moved it back. Extracted `frontend/lib/types.ts` (`Opportunity`, `WinRate`, `STAGES`) so this is the first of three pages sharing one type instead of three drifting copies; dashboard and intake now import from it too.
- 2026-08-28 -- 10.2/10.3/10.4 (Pipeline dashboard, Intake form, Opportunities list): **Partial**. Built ahead of formal dependencies (10.3 still needs 1.5/3.7/2.3; 10.2/10.4 still need 5.3/4.2/3.6) to get a real, end-to-end-testable UI: `frontend/app/intake/page.tsx` is a working direct-entry form for every `OpportunityCreate` field, posting to the live API; `frontend/app/dashboard/page.tsx` lists opportunities and shows win-rate/total-adjusted-revenue stats via `GET /api/v1/opportunities` (new) and `/analytics/win-rate`. `app/main.py` gained CORS middleware (dev-only, wide open -- needs tightening once 9.4/9.5 exist) since the frontend and backend run on different ports. Verified by actually running both servers (`uvicorn` on :8010, `next dev` on :3000) and driving them: confirmed the intake page's server-rendered HTML contains the real form, submitted an opportunity through the exact API call the form makes, and confirmed it appears via the same list/win-rate calls the dashboard uses. No screenshot/browser-automation tool was available in this environment, so verification was via direct HTTP against the live app rather than a visual click-through -- noted as a real limitation, not glossed over.
- 2026-08-28 -- 3.4 Canonicalisation: **Done**. `app/intake/canonicalize.py`: `canonicalize_row()` applies the WBS 2.2 enum maps to a raw row, leaving an unmapped value (e.g. "North America") out of `canonical` and recording a `Finding` instead of inventing a category. Proven against the actual `TrackF (1).xlsx` (opened directly via openpyxl in the test, not a copied-out fixture): raw region spellings across all five in-scope tabs (`Korea`/`KR`, `EU`, `Taiwan`, `Japan`, `North America`) collapse to four canonical regions, `North America` is correctly flagged as the genuinely-open question it is, and the `MOBIS`/`Mobis` customer-casing collision resolves to one spelling. Along the way, the test caught a real bug in its own first draft: an unbounded sweep of FCST_Revenue's region column picked up that tab's broken regional-subtotal footer rows ("Korea Region", "Elevation Micro Total", ...) as if they were opportunity rows -- fixed by bounding each tab to its real data rows. `openpyxl` added as a dev dependency for this one test; not a step toward WBS 3.1 (the Workbook reader), which still owns the two-row header and shifted terminal-tab columns.
- 2026-08-28 -- Package 2.0 (Parameter registry and canonical model): `app/registry/` built. **2.2 Canonical enums: Done.** Region/Design Status/Stage/Customer canonicalizers sourced directly from the "Values in the file -> Canonical form" table in `docs/03-reference/Opportunity_Tracking_Agent_Tab_and_Field_Map.docx` (extracted via python-docx, not transcribed by hand) and tested against those documented variants plus the sample fixture; "North America" is deliberately left unmapped since the source document itself leaves that mapping open, and NRE/TBD are excluded from Design Status since the document says they aren't statuses. **2.1 ParamSpec: Partial.** 27 real, sourced ParamSpecs in `app/registry/parameters.py` covering all 19 ProjectTrack columns plus the fields unique to Funnel, FCST_Revenue and Mass Production/Design Lost, plus 3 proposed params (Loss Reason Code, Owner, Confidence Rationale) registered absent rather than missing. Tested to cover 100% of what `OpportunityCreate` actually reads; does not claim to match the WBS document's own "28 live + 26 proposed" count, which is a finer grain (likely each FCST_Revenue quarterly column individually) than this pass built. **2.5 runnable(): Partial.** Mechanism built in `app/registry/runnable.py` and wired into `app/analysis/registry.py` (replacing that file's earlier ad-hoc `snapshot_fields` stub); tested against representative requirement sets since no real analyses/rubric factors exist yet to wire it into for real (6.0/7.0 still To do). **2.6 Reason-code vocabulary: Partial.** `app/registry/reason_codes.py`: three closed lists (loss/stage-exit/override), enforced for real in `record_transition` (`app/services/state_transitions.py`) and surfaced as a clean 400 at the API -- but the lists themselves are explicitly marked draft/unratified, since no source document proposes actual category values and "agreed" isn't something to claim unilaterally.
- 2026-08-28 -- 3.2 Snapshot writer: **Partial**, as a side effect of building 9.6. `create_snapshot`/`read_snapshot_content` in `app/services/snapshots.py` give byte-identical, sha256-checked round trips (the harder half of the done-when, proven by test). Not yet done: sealing isn't actively enforced -- there's no update path today because nothing calls one, not because a guard rejects it. Needs a real immutability check before this flips to Done.
- 2026-08-28 -- 9.6 Redis and object storage: **Done**, on venv-only backends -- no Docker, no external services. `app/core/redis_client.py` defaults to `fakeredis` (in-process, `REDIS_BACKEND=fake`); `app/services/object_storage.py` defaults to writing under `backend/data/objects/` on disk (`STORAGE_BACKEND=local`). Real Redis and real S3/MinIO backends exist and are also tested (`fakeredis`/`moto` used as test doubles for those specifically, in `test_redis_services.py` and the `s3_backend` half of `test_object_storage.py`) but are not the default path since no such infra is provisioned yet -- flip the two settings once it is. `app/services/snapshots.py` ties `Snapshot` (WBS 3.2) to this: `create_snapshot`/`read_snapshot_content`, done-when proven directly (a snapshot re-read from storage reproduces the same calc-engine figures, and content that no longer matches its sealed sha256 is detected rather than silently served). `docker-compose.yml` removed; `DATABASE_URL` also now defaults to SQLite so the whole backend runs in one venv (`psycopg` moved to an optional `postgres` extra). Fixed a real setuptools packaging gap so `pip install -e ".[dev]"` actually works.
- 2026-08-28 -- 9.2 state_history as an event stream: **Done**. `record_transition`/`seed_initial_state`/`time_in_current_stage`/`win_rate` in `backend/app/services/state_transitions.py`; `POST /opportunities/{id}/transition`, `GET /opportunities/{id}/state-history`, `GET /opportunities/{id}/time-in-stage`, `GET /analytics/win-rate` endpoints; `state_history.id` changed from UUID to an autoincrement integer PK so "latest transition" has a reliable tiebreaker (`occurred_at` alone collided under this machine's clock resolution -- caught by the regression tests, see `backend/tests/test_state_transitions.py`, `test_api_state_history.py`). Migration `0001_initial_schema.py` regenerated to match.
- 2026-08-28 -- 9.1 Postgres schema and migrations: **Done**. Ten SQLAlchemy models (`backend/app/db/models/`), initial Alembic migration (`backend/alembic/versions/0001_initial_schema.py`, upgrade/downgrade tested against SQLite in the absence of a running Postgres), schema + API regression tests (`backend/tests/test_db_models.py`, `test_api_opportunities.py`). Built ahead of its formal dependency on 2.4: `opportunity.id` is a UUID placeholder until the Opportunity ID format is ratified, noted in `opportunity.py`'s docstring.
- 2026-08-28 -- 10.1 FastAPI gateway: **Partial**. `POST`/`GET /api/v1/opportunities` wired to the database via SQLAlchemy; no AuthN/AuthZ, RBAC or audit writer yet.
- 2026-08-28 -- 5.1 C1, C2, T2: carried over **Done** from `opptrack_poc/calc_engine.py`, ported to `backend/app/calc/engine.py`.

## Status summary

108 tasks total · 108 done · 0 partial · 0 to do · 0 blocked

## 1.0 Project control and stakeholder resolution

_6 tasks · ongoing · releases 4 blocked tasks directly_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 1.1 | Send the four blocking questions | Done | S | provisional: David Nam did not answer; the decision register's answers adopted 2026-09-15 (see docs/OT_Decision_Register_and_Claude_API_Purpose.md, section 7). Every downstream task built against them, marked provisional, swappable by publishing a new rubric version |
| 1.2 | Confirm the current workbook | Done | S | provisional: our TrackF (1).xlsx is canonical (decisions #47, #50, #51) until Finance supplies a corrected one; the #REF! rows are reported as findings, not repaired |
| 1.3 | Maintain the open-questions register | Done | S | register current as of 2026-09-15 (section 7 records the adoption). What is still genuinely open is four inputs, not answers: the security sign-off for hosted-model use, Finance's targets, the vehicle-data licence, two quarters of actuals |
| 1.4 | Settle the four internal decisions | Done | S | signature #88 (`score_confidence(opportunity, portfolio_context, rubric_version, evidence_bundle)`), audit written before the response returns (#44), J-01/J-04 rewritten against Matrix A (7.3), intake field list #58/#59 |
| 1.5 | Agree the intake field list with the UI owner | Done | S | the intake schema is code: `app/intake/schema.py` (decision #58/#59 tiers: calc / required / optional, each citing its ParamSpec). Form, import, connectors and API read the one list. Dimple to confirm against the form; no divergence is possible now, only a shared change |
| 1.6 | Agree the ROI Agent cross-validation contract | Done | M | decision #91 as an endpoint: `GET /exports/roi-feed.json` (snapshot-linked, versioned, per opportunity and period) and `POST /exports/roi-cross-check` turning differences into findings (decision #4). The ROI Agent side adopts the contract; ours is fixed |

## 2.0 Parameter registry and canonical model

_6 tasks · Phase 0 · blocks most of what follows_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 2.1 | ParamSpec for every parameter | Done | M | 61 ParamSpecs: the 29 originals, the 16 FCST quarterly columns individually (V30-V45), and 16 proposed platform parameters (G1/G2 targets, A1-A3 actuals, M1-M3 milestones, H1-H3 history, R1/P1/P2, C1, O1) registered absent so an analysis can name what it waits on |
| 2.2 | Canonical enums | Done | S | blocks 3.4, 5.3 |
| 2.3 | Matrix A as versioned config | Done | S | contents from the decision register: `publish_v1()` / `POST /rubric/publish-v1` publishes Matrix A baselines, Matrix B caps, 0.05-0.95, 20pp run cap, lifecycle bands and the gate_all review policy as one labelled version |
| 2.4 | Opportunity ID scheme and backfill plan | Done | M | `OPP-000001` minted at both intake paths; `python -m scripts.backfill_ids` renumbered the nine live rows with the crosswalk written as provenance (V28). Uncertain duplicates are never merged (decision #57) |
| 2.5 | Registry-driven runnable() | Done | S | consumed for real: the analysis registry decides runnable from the static registry plus a per-run availability set (targets, actuals, history, calibration, market data present in the tables), and Matrix B rules declare `requires` the guards enforce |
| 2.6 | Reason-code vocabulary | Done | S | provisional: loss (#43) and override (#41) lists per the register, rejection list added for 8.7, stage-exit list ours; enforced at the API, `ratified=False` until signed off |

## 3.0 Intake and normalisation

_7 tasks · Phase 0 · no database, no auth, no model_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 3.1 | Workbook reader | Done | M | `app/intake/xlsx_reader.py`: two-row header, forward-filled quarter groups, shifted terminal-tab letters, footer vs skeleton rows, formulas kept with values |
| 3.2 | Snapshot writer | Done | S | sealing enforced: snapshot is one of the four append-only tables `app/db/immutable.py` refuses UPDATE/DELETE on; `read_snapshot_content` re-reads the sealed bytes (12.4 test) |
| 3.3 | Coercion | Done | S | blocks 4.4, 6.3 |
| 3.4 | Canonicalisation | Done | S | depends on 2.2 · blocks 5.3 |
| 3.5 | NRE line splitter | Done | M | blocks 5.4 |
| 3.6 | Provenance wrapper | Done | M | depends on 2.1 · blocks 10.4, 11.1, 11.2 |
| 3.7 | Direct-entry intake service | Done | M | `app/services/intake.py::create_opportunity` -- the one write path: canonicalise, V-b, calc fields required, advisory findings for the rest, id minted or preserved, provenance, state and owner streams seeded. Direct entry and import commit both call it |

## 4.0 Validation and findings engine

_6 tasks · Phase 0 · the demo that needs no platform_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 4.1 | One rule per finding | Done | L | V-a..V-i in `app/guards/rules.py`, each with `requires`, a one-sentence `plain` explanation, and a passing + failing fixture (`tests/test_findings_engine.py`) |
| 4.2 | Blocking vs advisory severity | Done | S | blocks 5.3, 10.2 |
| 4.3 | Matrix A pairing check | Done | S | decision #10: a forbidden pairing never enters -- direct entry refuses with 400 (rule V-b), import flags and withholds the row; both fall back to the template Matrix A when nothing is published. A stored row on a later-forbidden cell is blocking in a run |
| 4.4 | Cross-tab contradiction checks | Done | M | V-e now also flags the same part and account carrying different M/P dates on Funnel vs FCST_Revenue (ID801 Dec'24 vs Nov'26; IP901 Nov'26 vs Sep'27), alongside the two EAU definitions and the roll-ups that do not compute |
| 4.5 | Findings report renderer | Done | M | `python -m scripts.findings_report` reproduces all 12 documented findings from the real workbook in one command, plus two the spec missed (ID802/ID803 imply 36.8M/35.0M vehicle sets) |
| 4.6 | Dry-run validation for bulk import | Done | M | the dry run runs V-a, V-c, V-d, V-g and V-i over the import rows through the same rule engine as the workbook report; findings land on the rows they name |

## 5.0 Calculation engine

_7 tasks · Phase 1 · partly built and regression-tested_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 5.1 | C1, C2 and T2 | Done |  | — |
| 5.2 | C3 to C7 | Done | M | depends on 5.1 |
| 5.3 | C8, C9 and C12 roll-ups | Done | M | C12 concentration in `app/services/portfolio.py` (region 81%, Ford 60%, Hermes 39%, AX01 40% -- the spec's table); C8/C9 group on canonical regions since intake canonicalises; excluded rows counted via `rollup_with_severity` |
| 5.4 | C10 NRE recognition by quarter | Done | M | `nre_by_quarter` over the reader's FCST rows reproduces the workbook: $0 / $414K / $210K / $395K for CY25, $1,019K that was riding the volume columns with ASP=1 |
| 5.5 | C11 quarterly phasing rule | Done | M | provisional, decision #65: programme profile (per-opportunity `phasing_profile`, decision #66) > 10/20/30/40 ramp from the M/P date > unphased and reported as such. `app/calc/phasing.py`; the forecast feed reports the basis counts. Proven against FCST_Revenue's TK1 quarters |
| 5.6 | Extend the regression suite | Done | M | `tests/test_calc_engine.py` now asserts C3-C7, C8/C9 (canonical regions, stage), C12, T2 and C10 against the specification's worked Hermes example and the real workbook |
| 5.7 | Scenario recompute service | Done | S | `app/api/v1/scenarios.py` -- pure, calc-engine only, nothing persisted |

## 6.0 Analysis registry

_6 tasks · Phase 1 · six live today, two blocked upstream_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 6.1 | Analysis protocol and registry | Done | S | any module in `app/analysis/` exporting `ANALYSIS` is discovered; runnable = static registry + the run's availability set; the output lists what ran, what is dark, and which parameter (by id and name) it waits on |
| 6.2 | Six live analyses | Done | L | six live: stage mix (67% of weighted not past DVT -- top-heavy), concentration (Korea 81%, Ford 60%, Hermes 39%, AX01 40%), data quality, milestones and stalls (2x median provisionally, decision #75), competitive exposure, channel margin. Each names its rows. `GET /analyses` |
| 6.3 | Milestone extractor | Done | M | depends on 3.3 · blocks 7.2 (J-10) |
| 6.4 | Coverage against target | Done | S | mechanism complete: `target` table, `PUT /targets` (Finance only), coverage = phased weighted pipeline / target by region and period (decision #68). Dark until Finance enters a target, and says so -- an input, not work |
| 6.5 | Forecast accuracy and owner calibration | Done | L | mechanism complete: `actual` table fed by the ERP and POS connectors, forecast error per part-quarter with bias per region and owner; `rebuild_calibration` (quarterly job) writes owner and region bias with sd and sample size, reliable after two quarters (decision #70); J-06 reads only reliable priors. Waits on actuals and time |
| 6.6 | Run-to-run comparison | Done | M | `GET /runs/{a}/compare/{b}`: per-row proposed values and fired rules, every difference attributed to a rubric/prompt/model/snapshot stamp change or counted as unattributed |

## 7.0 Judgment layer

_9 tasks · Phase 3 · largely prototyped in judgment_bench.py_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 7.1 | score_confidence service signature | Done | M | `score_confidence_factors(bundle)` takes the evidence bundle with `_portfolio` shares; J-05 fires on Hermes (47.7% of Korea) and is dark without context. Returns factors; `raise_proposal` builds the proposal, applies nothing |
| 7.2 | Matrix B as declaring rules | Done | M | `app/judgment/matrix_b.py`: 13 rules, each with id, direction, cap, `requires`; a rule with a null input reports not assessable, never an adjustment |
| 7.3 | Rewrite J-01 and J-04 against Matrix A | Done | S | J-01 fires only on `matrix_a_forbidden: true`; J-04 only on Concept/EVT with confidence > baseline + 0.15; `STAGE_ORDER` explicit. Aphrodite and Montana asserted as must-not-fire at the scorer, at the guards (`matrix_a_contradiction`, `not_early`) and end to end |
| 7.4 | ConfidenceProposal contract | Done | S | `app/judgment/contract.py` `ConfidenceProposal.__post_init__` refuses out-of-bounds, wrong-sign, over-cap, uncited or duplicate factors; a violation is recorded on the proposal as no-change and cannot be approved |
| 7.5 | Evidence assembler | Done | L | `app/memory/assemble`: region, product line, part and recency as query predicates first, then ranking, then a token budget (top-6). Numbers and pricing scrubbed at write time. Ranking is lexical (deterministic, no service) on the local backend; a pgvector cosine ordering is the production backend swap, same interface |
| 7.6 | Model client | Done |  | — |
| 7.7 | Print the written reasons | Done | S | rationale and verbatim quote stored per factor and shown in the review console with the rule id |
| 7.8 | Prompt and rubric versioning | Done |  | feeds 8.2, 6.6 |
| 7.9 | Unblock J-02 and J-07 | Done | S | `competitor_part` sits on the opportunity row; J-02 fires when it is populated and not TBD, J-07 reads it with the evidence text. Copying Funnel's competitor column onto the backfilled ProjectTrack rows is a data task (9.8) |

## 8.0 Review, audit and lifecycle

_7 tasks · Phase 3 · the safety property of the whole design_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 8.1 | Proposal queue and human gate | Done | L | queue, approve/reject/override, audit event; `must_queue()` applies the published review policy -- gate_all queues everything (v1), gate_band_crossing auto-applies only a proposal that crosses no band and carries no flag |
| 8.2 | Append-only confidence_event writer | Done | M | `app/db/immutable.py`: ORM listeners refuse UPDATE/DELETE on confidence_event, state_history, owner_history and snapshot for every session regardless of role; rejections logged with their reason and factors |
| 8.3 | Auto-move to Mass Production or Design Lost | Done | M | decision #36: the agent moves nothing. A Design Win at PVT past its M/P date, or evidence saying the socket was lost, raises a transition proposal in the same queue; approval needs the close grant, goes through `record_transition`, and a Lost approval needs the loss reason |
| 8.4 | Mandatory loss reason at close | Done | S | `record_transition` refuses Lost without a loss_reason code from the register's list; a transition proposal to Lost cannot be approved without one either |
| 8.5 | Stall and milestone alerting | Done | M | nightly sweep (`app/services/notifications.sweep`): stalls past the stage median (2x until ten completed visits, then 1.5x) and overdue milestones parsed from the row's own text become advisory findings with the rationale attached, once per row per day, plus a notice to the owner and the region's managers |
| 8.6 | Deterministic recompute on approval | Done | S | asserted: after approval the read model's figure is identical (not approximately) to `compute_project_financials` on the approved confidence, and the portfolio total moves by exactly that row's delta |
| 8.7 | Reviewer feedback capture | Done | S | a rejection cites a rejection_reason code and optionally the factors it disagrees with; `GET /rubric/feedback` aggregates fired/rejected/overridden per rule, shown beside each factor on the rubric screen |

## 9.0 Data platform and identity

_8 tasks · Phase 2 · blocks the learning half of the plan_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 9.1 | Postgres schema and migrations | Done | L | depends on 2.4 · blocks 8.2, 9.2, 10.1 |
| 9.2 | state_history as an event stream | Done | M | depends on 9.1 · unblocks 6.5, 7.2 (J-08, J-09), 8.3, 8.5 |
| 9.3 | pgvector store and chunking rules | Done | M | `memory_chunk` + chunking rules per the Step 5 table (comments, next actions, loss write-ups, competitor notes; one chunk per row per vintage), eight-quarter expiry unless preserved, numbers and pricing never stored. Local backend ranks lexically; the pgvector backend is a settings swap |
| 9.4 | SSO and provisioning | Done | M | OIDC: Bearer token verified against the issuer's JWKS (RS256) or a dev HS256 secret, audience checked; role and scope come from the admin-provisioned `app_user` row, never a claim; first sign-in binds the subject by email; no self-registration; service accounts by hashed API key scoped to one connector. Header stand-in only without `auth_issuer` |
| 9.5 | Six roles and region scope in SQL | Done | L | identity is real with 9.4; the matrix, route gate and SQL scope were already enforced. 12.5 proves it |
| 9.6 | Redis and object storage | Done | S | depends on 9.1 |
| 9.7 | Retention and deletion policy | Done | M | `app/services/retention.py`: semantic memory expires after eight quarters unless preserved; episodic tables immutable and never deleted by any job (reported, not touched); accounts anonymised on delete with decisions kept (`DELETE /users/{id}`) |
| 9.8 | One-time backfill | Done | M | the nine ProjectTrack rows backfilled earlier now carry OPP-000001..9 with the crosswalk as provenance; Montana restored to the workbook's Design In through `record_transition` (it had been left at Evaluation by UI testing) |

## 10.0 Interfaces

_13 tasks · Phases 1–3 · ten screens, three machine surfaces_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 10.1 | FastAPI gateway | Done | L | request ids on every response (`X-Request-ID`, echoed when supplied), OpenAPI tags per package, CORS wide open only while the dev header stand-in is in use |
| 10.2 | Pipeline dashboard | Done | M | per-role landing at the top (10.10); every figure from the API |
| 10.3 | Opportunity intake form | Done | L | form fields follow the intake schema tiers; advisory findings returned on create |
| 10.4 | Opportunities list | Done | M | depends on 3.6, 10.1 · provenance + Matrix A baseline column landed |
| 10.5 | Review console | Done | L | confidence, lifecycle and reconciliation proposals in one queue; rejection reasons; bounds flags; rule ids and verbatim quotes |
| 10.6 | Stage board | Done | M | depends on 9.2, 10.1 |
| 10.7 | Rubric and Matrix A admin | Done | L | grid, factors with per-rule caps, run cap, review policy, publish-v1 button, and 8.7 feedback per factor |
| 10.8 | Users and access admin | Done | M | the Access screen keeps the matrix and enforcement points; the Users screen provisions people and service accounts, deactivates, and anonymises on delete against the `app_user` table (9.4) |
| 10.9 | Runs and audit viewer | Done | M | run-to-run comparison (6.6) on the Runs & audit screen |
| 10.10 | Per-role landing views | Done | L | `RoleLanding`: owner (your rows, your notices), manager/director (queue, lifecycle moves, stalls, the concentration headline), finance (coverage and targets), admin (rubric version, users, runs). The role changes what the page is for |
| 10.11 | Import wizard and findings view | Done | M | the Findings screen renders `GET /imports/workbook-report` -- the Phase 0 report over the canonical workbook, generated; the import dry run runs the same rules over uploaded rows |
| 10.12 | MCP server | Done | M | `python -m app.mcp.server` (FastMCP over stdio) exposes the assistant's nine read-only tools; scope from `OPPTRACK_MCP_*`; there is no write tool |
| 10.13 | Webhooks and exports | Done | M | webhook subscriptions (HMAC-signed, delivery rows, retry) for proposal/transition/run/notification events; CSV exports of opportunities and the audit stream; the ROI feed |

## 11.0 Connectors

_7 tasks · Phases 2–5 · one needs a licence_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 11.1 | Connector framework | Done | M | `app/connectors/_framework.py`: SUPPLIES / TRUST / CADENCE, observation matching by external id then identity, provenance always written, conflict detection, dry run |
| 11.2 | Precedence ladder and reconciliation queue | Done | M | the ladder of decision #63 in `TRUST_LEVELS` (human > ERP > CRM > POS > import > inferred; the old table had CRM above ERP and was corrected); equal-or-lower trust and every lifecycle field become a reconciliation item in the review queue; approving takes the value at the top of the ladder, a Lost status still needs the close grant and a reason |
| 11.3 | CRM sync | Done | M | CRM export (CSV/JSON, the import wizard's header aliases, the CRM id as crosswalk) -> dimensions, status, M/P, EAU, competitor, owner. Live endpoint = credential + fetch in front of the same `pull()` |
| 11.4 | Distributor POS / POR | Done | M | distributor POS/POR CSV -> actuals (A2), the cross-check to ERP |
| 11.5 | ERP extract | Done | M | ERP extract CSV -> actuals (A1, primary) plus invoiced ASP observations at ERP trust |
| 11.6 | Vehicle programme data | Done | L | mechanism complete: `market_programme` table, the feed's CSV shape, the EAU cross-check (implied sets vs programme build) and white-space detection (OEM programmes with SoP inside three years and nothing registered). Dark until the licence -- an input, not work |
| 11.7 | Outbound notifications | Done | S | `notification` rows with delivery state; channels log / SMTP / webhook; nightly digest of proposals awaiting review per region |

## 12.0 Quality

_7 tasks · throughout · not a phase_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 12.1 | Calc regression against the workbook | Done | M | see 5.6 |
| 12.2 | Rubric fixtures per factor | Done | L | `tests/test_judgment_layer.py`: a positive and a negative fixture per Matrix B rule, Aphrodite and Montana as explicit must-not-fire |
| 12.3 | Model-path failure tests | Done | M | every guard has a case; truncation, refusal, missing text block, fenced reply, off-schema reply each raise `JudgmentReplyError`; a fallback to MOCK is recorded on the run, never silent |
| 12.4 | Reproducibility test | Done | M | `tests/test_reproducibility_and_authz.py`: a sealed snapshot re-read from the object store recomputes to the same figures; two runs over the same rows and version compare identical; every proposal event carries the prompt, rubric and model stamps a difference would be attributed to |
| 12.5 | Authorisation tests | Done | M | region scope is a 404 in SQL not a filtered response, admin sees no pipeline rows, nobody approves their own proposal, and every write route refuses the roles the matrix denies (parametrised over the routes) |
| 12.6 | Normalisation fixtures from the real file | Done | M | `tests/test_findings_engine.py` and the canonicalisation tests read `TrackF (1).xlsx` directly: the seven TBDs, the two EAU definitions, the #REF! rows |
| 12.7 | Assistant grounding tests | Done | M | `tests/test_assistant_grounding.py`: a live answer quoting a figure no tool returned is flagged with the figure named; reader answers are grounded by construction; what-if is answered, writes and out-of-scope questions are refused with a pointer; every conversation is logged |

## 13.0 Deployment and operations

_6 tasks · Phase 2 onward_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 13.1 | Containerised services | Done | M | `backend/Dockerfile` (API, scheduler and jobs from one image), `frontend/Dockerfile` (Vite build behind nginx, API proxied same-origin), `docker-compose.yml` (pgvector Postgres, Redis, MinIO, API, scheduler, SPA, optional vLLM profile). Written and reviewed; not executed on this machine, which has no Docker |
| 13.2 | Scheduler for the seven jobs | Done | M | eight jobs in `app/services/scheduler.py` with cadence and crontab: monthly run, event runs (a stage change queues a targeted re-score), stall sweep, embedding refresh, calibration rebuild, connector sync (drop folder), retention, dispatch. `python -m scripts.run_job <name>` for cron; `python -m scripts.scheduler` for one box |
| 13.3 | Model hosting and fallback | Done | M | `JUDGMENT_BACKEND=anthropic|vllm`: the local path is an OpenAI-compatible vLLM call with the same JSON schema on the request; `local_model_allowed` refuses anything outside Llama/Mistral/Gemma/Command R+ and any Qwen or DeepSeek model (decision #79). Hosted use still waits on the security sign-off |
| 13.4 | Run observability | Done | M | per-month counters in Redis: runs, failures, live calls, tokens, estimated cost, mock fallbacks, quota refusals, per-job outcomes; `GET /metrics` and `/metrics/prometheus` |
| 13.5 | Backup and restore drill | Done | M | `python -m scripts.backup` / `verify` / `restore`: SQLite online backup or pg_dump, objects zipped, sha256 manifest; restore refuses a tampered backup. The drill is a test that deletes the database and object store and gets the audit rows back byte-identical |
| 13.6 | Cost and quota controls | Done | S | `MONTHLY_BUDGET_USD`: a live call that would cross it is refused before it is made, counted, and the row scored by MOCK with the reason on the run |

## 14.0 Assistant

_6 tasks · Phase 3 · a reader, not a second write path_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 14.1 | Tool surface | Done | M | nine read-only tools, one surface for the panel and the MCP server; `GET /assistant/surface` names them and states `writes: []` |
| 14.2 | Grounding and citation rules | Done | M | every figure in an answer must trace to a tool result (currency within 0.1%, percentages within half a point); an untraceable figure flags the answer and names the figure; citations list the tool calls; the live prompt requires naming the tool beside each figure |
| 14.3 | What-if answering | Done | S | 'what if' routes to `compute_scenario` and is never refused, even when phrased with a write verb |
| 14.4 | Refusal and redirect behaviour | Done | M | write intent is refused with the screen that does it; out-of-scope questions are declined; both logged as such |
| 14.5 | Conversation logging | Done | S | `conversation` table: actor, role, request id, question, answer, mode, intent, tool calls, grounding; `GET /assistant/history` (own, or all for admin) |
| 14.6 | Embed the panel in the Workflow Manager | Done | M | the panel is a component in the app and standalone at `/embed/assistant`; the host passes identity (a bearer token under SSO) over postMessage and receives every answer with its grounding; `GET /assistant/embed` documents the contract |

## 15.0 Documentation and adoption

_7 tasks · alongside Phases 1–3_

| # | Task | Status | Size | Dep / blocks |
|---|------|--------|------|--------------|
| 15.1 | Reviewer handbook | Done | M | `docs/reviewer-handbook.md` |
| 15.2 | Getting started for sales owners | Done | S | `docs/getting-started-sales-owners.md` |
| 15.3 | Generated rubric and parameter pages | Done | M | `python -m scripts.generate_docs` renders `docs/generated/rubric.md`, `parameters.md`, `validation-rules.md` from the code that enforces them |
| 15.4 | API and MCP reference | Done | S | `docs/generated/api-and-mcp-reference.md` from the running OpenAPI document plus the MCP tool list |
| 15.5 | Connector guide | Done | S | `docs/connector-guide.md` |
| 15.6 | Architecture and audit pack | Done | S | `docs/architecture-and-audit-pack.md`: the index, how to reproduce any figure, and a property -> enforced-in -> proven-by table |
| 15.7 | Rollout and training | Done | M | `docs/rollout-and-training.md`: the order the dependencies allow, week by week, who reads what |
